#include "mbed.h"

// main() runs in its own thread in the OS
int main()

//Initalize variables

{
    int a = 10;
    int b = 2;
    int c = 30;
    int d, e;

    //add values of a and b and store in d

    d = a + b;
    printf("Value of d: %d/n", d);


    //subtract value of c from d and store it in e

    e = d - c;
    printf("Value of e: %d/n", e);

    //End program
    return 0;
    {

    }
}

